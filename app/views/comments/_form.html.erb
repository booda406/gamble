<%= simple_form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this turtle from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="respond_box">
  <div class="row respond_inbox">
    <div class="col-xs-12 col-sm-4 col-md-4 col-lg-3 adjust-left">
      名字:
     <%= f.input_field :name, :required => true, :label => "姓名:", input_html: { class: "form-control" }, :value => current_user.name, :disabled => true %>
    </div>
  </div>
  <div class="row respond_inbox">
    <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6 adjust-left">
      <h4>想說的話</h4>
      <%= f.text_area :content, :required => true, :class => "form-control", :style => "overflow:visible" %>
    </div>
  </div>
    <%= f.hidden_field :event_id, :value => params[:id] %>
  <div class="actions">
    <%= f.submit "送出" %>
  </div>

</div>
<% end %>